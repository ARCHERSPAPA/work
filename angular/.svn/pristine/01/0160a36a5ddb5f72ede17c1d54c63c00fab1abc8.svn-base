import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ScheduleStayComponent } from './schedule-stay.component';

describe('ScheduleStayComponent', () => {
  let component: ScheduleStayComponent;
  let fixture: ComponentFixture<ScheduleStayComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ScheduleStayComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ScheduleStayComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
