import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TempLibListComponent } from './temp-lib-list.component';

describe('TempLibListComponent', () => {
  let component: TempLibListComponent;
  let fixture: ComponentFixture<TempLibListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TempLibListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TempLibListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
