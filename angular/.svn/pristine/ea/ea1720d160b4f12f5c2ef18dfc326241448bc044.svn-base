import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CostBaseComponent } from './cost-base.component';

describe('CostBaseComponent', () => {
  let component: CostBaseComponent;
  let fixture: ComponentFixture<CostBaseComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CostBaseComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CostBaseComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
