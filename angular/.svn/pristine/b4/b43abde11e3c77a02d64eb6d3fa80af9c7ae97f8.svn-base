import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TempSuitEditComponent } from './temp-suit-edit.component';

describe('TempSuitEditComponent', () => {
  let component: TempSuitEditComponent;
  let fixture: ComponentFixture<TempSuitEditComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TempSuitEditComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TempSuitEditComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
