import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MasterPromotionListComponent } from './master-promotion-list.component';

describe('MasterPromotionListComponent', () => {
  let component: MasterPromotionListComponent;
  let fixture: ComponentFixture<MasterPromotionListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MasterPromotionListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MasterPromotionListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
