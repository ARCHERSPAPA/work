import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ArticleExceptionListComponent } from './article-exception-list.component';

describe('ArticleExceptionListComponent', () => {
  let component: ArticleExceptionListComponent;
  let fixture: ComponentFixture<ArticleExceptionListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ArticleExceptionListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ArticleExceptionListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
