import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { NoticeInformComponent } from './notice-inform.component';

describe('NoticeInformComponent', () => {
  let component: NoticeInformComponent;
  let fixture: ComponentFixture<NoticeInformComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ NoticeInformComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(NoticeInformComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
